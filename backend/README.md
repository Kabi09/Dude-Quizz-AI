# Quiz Backend
Express + Mongoose backend.

- Put your JSON files into `data/` (already includes uploaded JSONs if present).
- Run `npm install` and then `npm run seed` to load data into MongoDB.
- Start server with `npm start`. API base: http://localhost:4000/api
